import React from "react";
import styles from "../styles/Components/header.module.css";

const Header = () => {
  return (
    <header className={styles.header}>
      <h1 className={styles.title}>Campus Currency</h1>
    </header>
  );
};

export default Header;
